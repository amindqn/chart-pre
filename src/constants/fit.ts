import type { SeriesFitType } from '../types/plot';

export const DEFAULT_FIT_TYPE: SeriesFitType = 'linear';
export const DEFAULT_FIT_SAMPLE_COUNT = 120;

export const FIT_SAMPLE_COUNT_OPTIONS = [50, 100, 150, 200];
